# Database - Neon.tech (Cloud PostgreSQL)
PGUSER=admin
PGPASSWORD=npg_TokODPY2bS4f
PGHOST=ep-still-band-aiqm10i0-pooler.c-4.us-east-1.aws.neon.tech
PGPORT=5432
PGDATABASE=flipthetable

# Or use full connection string:
# DATABASE_URL=postgresql://admin:npg_TokODPY2bS4f@ep-still-band-aiqm10i0-pooler.c-4.us-east-1.aws.neon.tech/flipthetable?sslmode=require&channel_binding=require

# JWT
JWT_SECRET=your-secret-key-change-this-in-production

# Server
PORT=8080

# Etsy API (used for the seller-panel Etsy import)
ETSY_API_KEY=
ETSY_SHARED_SECRET=
# Must exactly match a Redirect URI registered in your Etsy app settings
ETSY_OAUTH_REDIRECT_URI=http://localhost:8080/seller/etsy/oauth/callback
# Where sellers land after connecting/denying access on Etsy's consent screen
FRONTEND_URL=http://localhost:3000

# Stripe (payment provider) — use your test-mode keys from the Stripe Dashboard
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
# Signing secret for the /webhooks/stripe endpoint (from `stripe listen` in dev, or the webhook's settings page in prod)
STRIPE_WEBHOOK_SECRET=
